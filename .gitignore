.Rproj.user
.Rhistory
.RData
.Ruserdata
themes
scraps.R
.lintr

# data caches: regenerate with `Rscript update_data.R`
*.rds

# rendered output: ~8 MB per render and git cannot delta-compress it.
# published straight from disk with `quarto publish posit-connect-cloud --no-render`
*.html

# ESPN crests, fetched once and cached for the franchise chart
logos/
