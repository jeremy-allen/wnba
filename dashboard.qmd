--- 
title: "Meet the WNBA"
format:
  dashboard:
    scrolling: true
    embed-resources: true
theme:
  - united
  - styles.scss
---


```{r packages}
library(sparkline)
library(htmltools)
library(dplyr)
library(tidyr)
library(ggplot2)
library(lubridate)
library(stringr)
library(wehoop)
library(ggrepel)
library(reactable)
library(purrr)
library(plotly)
library(crosstalk)
# required by wehoop: pre-2024 season .rds files store their columns as arrow
# ALTREP vectors, which readRDS silently returns as length-zero vectors (and
# rbindlist then pads to NA) unless arrow is loaded
library(arrow)
```


```{r data}
this_year <- as.integer(year(now()))

seasons <- c(2004:as.integer(this_year))

# Load the configuration
config <- config::get()

# get data based on the configuration

if (config$data_source == "local") {
    dat <- readRDS("dat.rds")
    sched <- readRDS("sched.rds")
}
  
if (config$data_source == "wehoop") {
    dat <- load_wnba_player_box(seasons = seasons)
    sched <- wehoop::load_wnba_schedule(seasons = seasons)
}

sched_games <- sched |> 
  select(game_id, game_date, season_type, type_id, type_abbreviation,
  home_display_name, home_score, away_display_name, away_score, attendance) |> 
    mutate(attendance = as.integer(attendance))

# game types
game_types <- sched_games |> 
  select(game_id, type_id, type_abbreviation)

# reguarl season games excluding CC and Allstar games
rs_game_ids <- sched_games |> 
  select(game_id, type_id) |> 
  filter(type_id == 1)

# postseason games
ps_games_ids <- sched_games |> 
  select(game_id, type_id) |> 
  filter(type_id %in% c(14, 16, 17))

athletes <- dat |>
  select(athlete_id, athlete_display_name)

# Drop All-Star exhibition rosters, which are not real teams. This used to be
# `filter(team_id < 90)`, back when every franchise had a two-digit id and only
# the exhibition rosters did not. ESPN now issues six-digit ids to new
# franchises too, so that test also removed Golden State, Toronto and Portland.
# Exclude the exhibitions by game type instead, which is what was always meant.
dat <- dat |>
  left_join(game_types, by = "game_id") |>
  filter(type_id != 4) |>
  # Drop players who dressed but did not play. ESPN carries a row per rostered
  # player per game; the inactive ones have no minutes and no stats, but they
  # still inflate every n_distinct(game_id) used as a games-played denominator.
  # That is what put this dashboard's per-game averages under ESPN's -- Caitlin
  # Clark sat 3 games, which read as 19.3 ppg here against ESPN's 21.5.
  #
  # The `did_not_play` flag only became reliable in 2013. Before that, 10-22%
  # of rows per season are inactive players it never marked; they show up as a
  # row with no minutes and every stat column NA. `!is.na(points)` catches
  # exactly those 7,654 rows and nothing else -- of 102,076 rows that do carry
  # minutes, not one is blank across all thirteen stat columns.
  #
  # Together these keep "one row = one game actually played" true downstream.
  filter(!(did_not_play %in% TRUE), !is.na(points))

games <- dat |>
  filter(season == this_year) |>
  summarise(
    team_1 = first(team_display_name),
    team_2 = first(opponent_team_display_name),
    .by = c(game_id, game_date)
  ) |> 
    arrange(desc(game_date))

# max number of games this year for any team
num_games <-dat |>
  filter(
    season == this_year,
    type_id == 1
    ) |>
  select(team_display_name, game_id, game_date) |>
  unique() |> 
  arrange(team_display_name, desc(game_date)) |> 
  count(team_display_name, sort = TRUE) |> 
  head(1) |>
  pull(n)

```

```{r freshness}
#| output: asis

# The dashboard is rendered and published by hand, so it carries no other
# signal of how current it is. Report both dates: the render date alone would
# imply the data is as fresh as the render, which it is not.

fmt_date <- function(d) {
  d <- as.Date(d)
  paste0(format(d, "%b"), " ", as.integer(format(d, "%d")), ", ", format(d, "%Y"))
}

freshness_stamp <- paste0(
  "Data through ", fmt_date(max(dat$game_date, na.rm = TRUE)),
  "  ·  Rendered ", fmt_date(Sys.Date())
)

# The navbar is built by Quarto after this chunk runs, so append to it on load.
# The tab list carries Bootstrap's `me-auto`, which right-aligns any sibling
# placed after it.
cat(sprintf(
  '<script>
document.addEventListener("DOMContentLoaded", function () {
  var nav = document.querySelector("#dashboard-collapse");
  if (!nav) return;
  var stamp = document.createElement("span");
  stamp.className = "navbar-stamp";
  stamp.textContent = "%s";
  nav.appendChild(stamp);
});
</script>',
  freshness_stamp
))
```

```{r funcs1}
post_season_ids <- c(14, 16, 17)

player_stats <- function(
  data, season_part = c("regular", "post"), my_season = NULL
  ) {
  
  # stop if no type_id column
  stopifnot("type_id" %in% names(data))

  season_part <- match.arg(season_part)

  my_type_id <- as.integer(1)

  if (season_part == "post") {
    my_type_id <- post_season_ids
  }

  result <- data |> 
    filter(
      !is.na(athlete_display_name),
      type_id %in% my_type_id
      )

  if (!is.null(my_season)) {
    result <- result |> 
      filter(season == my_season)
  }
  
  types <- result$type_id |> unique()

  result <- result |>
    replace_na(list(points = 0)) |>
    summarise(
      position = paste(
        unique(athlete_position_name),
        collapse = ", "
      ),
      total_games = if_else(
        nchar(n_distinct(game_id, na.rm = TRUE)) == 1,
        as.integer(paste0("0", n_distinct(game_id, na.rm = TRUE))),
        as.integer((n_distinct(game_id, na.rm = TRUE)))
      ),
      total_seasons = if_else(
        !is.null(my_season),
        1,
        as.integer(n_distinct(season, na.rm = TRUE))
      ),
      seasons = if_else(
        n_distinct(season, na.rm = TRUE) != 1,
        paste(range(season, na.rm = TRUE), collapse = "-"),
        as.character(range(season, na.rm = TRUE), collapse = "-")[[1]]
      ),
      latest_team = last(team_display_name),
      total_points = sum(points, na.rm = TRUE),
      ppg = list(points), # list for sparkline plots in table rows
      goals_made = sum(field_goals_made, na.rm = TRUE),
      threes_made = sum(three_point_field_goals_made, na.rm = TRUE),
      frees_made = sum(free_throws_made, na.rm = TRUE),
      total_assists = sum(assists, na.rm = TRUE),
      total_rebounds = sum(rebounds, na.rm = TRUE),
      total_steals = sum(steals, na.rm = TRUE),
      total_blocks = sum(blocks, na.rm = TRUE),
      total_turnovers = sum(turnovers, na.rm = TRUE),
      total_fouls = sum(fouls, na.rm = TRUE),
      total_ejections = sum(ejected, na.rm = TRUE),
      .by = athlete_display_name
    ) |>
    rename(
      athlete = athlete_display_name
    ) |>
    arrange(desc(total_points))

  if (!is.null(my_season)) {
    result |>
      select(-total_seasons)
  } else {
    result
  }
}

# player points per season over entire career
player_pps <- function(data, my_type_id = 1) {

  data |>
    filter(type_id == my_type_id) |>
    replace_na(list(points = 0)) |>
    summarise(
      points = as.integer(sum(points, na.rm = TRUE)),
      .by = c(athlete_display_name, season)
    ) |>
    arrange(athlete_display_name, season)
}
```

```{r funcs2}
player_doubles <- function(data, my_type_id = 1, my_season = NULL) {
  dat <- data |>
    filter(type_id == my_type_id)

  if (!is.null(my_season)) {
    dat <- dat %>%
      filter(season == my_season)
  }

  dat |>
    select(
      athlete = athlete_display_name,
      game_id,
      points,
      rebounds,
      assists,
      steals,
      blocks
    ) |>
    mutate(game_id = as.character(game_id)) |>
    arrange(game_id) |>
    mutate(
      across(
        points:blocks,
        \(x) if_else(is.na(x) | x < 10, FALSE, TRUE)
      )
    ) |>
    mutate(
      doubles = as.integer(
        rowSums(
          pick(where(
            ~ is.logical(.x)
          ))
        )
      )
    ) |>
    mutate(
      doubles_type = case_when(
        doubles == 2 ~ "double_double",
        doubles == 3 ~ "triple_double",
        doubles == 4 ~ "quadruple_double",
        doubles == 5 ~ "quintuple_double",
        .default = "other"
      )
    ) |>
    select(game_id, athlete, doubles_type) |>
    count(athlete, doubles_type) |>
    pivot_wider(
      names_from = doubles_type,
      values_from = n
    ) |>
    replace_na(
      list(double_double = 0, triple_double = 0)
    ) |>
    mutate(
      double_double = if_else(
        triple_double > 0, double_double + triple_double, double_double
      )
    ) |>
    select(athlete, triple_double, double_double)
}
```

```{r funcs3}

stat <- function(data, col, ...) {
  if (n_distinct(data$seasons) > 1) {
    data |>
      select(athlete, {{ col }}, total_seasons, seasons) |>
      arrange(desc(.data[[col]])) |>
      slice(1:150) |>
      mutate(rank = row_number()) |>
      select(rank, athlete, {{ col }}, total_seasons, seasons)
  } else if (n_distinct(data$seasons) == 1) {
    data |>
      select(athlete, {{ col }}, seasons) |>
      arrange(desc(.data[[col]])) |>
      slice(1:150) |>
      mutate(rank = row_number()) |>
      select(rank, athlete, {{ col }}, seasons)
  }
}
```

```{r dataprep}

pps <- dat |>
  player_pps() |>
  arrange(athlete_display_name, season) |>
  summarise(
    pps = list(points),
    .by = athlete_display_name
  )

pdat <- player_stats(data = dat, season_part = "regular")

player_seasons <- pdat |>
  select(athlete, total_games, total_seasons, seasons) |> 
  mutate(
    across(
      where(is.double),
      as.integer
    )
  )

doubles <- dat |>
  player_doubles()

main_stats <- pdat |>
  select(
    athlete,
    total_points,
    # total_assists,
    # total_rebounds,
    # total_steals,
    # total_blocks,
    position,
    latest_team
  )

doubles <- doubles |>
  left_join(main_stats, by = join_by(athlete)) |>
  left_join(pps, by = join_by(athlete == athlete_display_name)) |>
  left_join(
    player_seasons,
    by = join_by(athlete)
  ) |>
  mutate(rookie = if_else(seasons == year(now()), "rookie", ""))  |> 
  arrange(desc(total_points)) |>
  select(athlete, rookie, triple_double, double_double, total_points, pps, everything())

doubles <- SharedData$new(doubles, group = "experience", key = ~athlete)

```

```{r data24prep}
pdat24 <- player_stats(data = dat, season_part = "regular", my_season = this_year)
doubles24 <- player_doubles(data = dat, my_season = this_year)

main_stats24 <- pdat24 |>
  select(
    athlete,
    total_points,
    ppg,
    # total_assists,
    # total_rebounds,
    # total_steals,
    # total_blocks,
    position,
    latest_team
  )

doubles24 <- doubles24 |>
  left_join(main_stats24, by = join_by(athlete)) |>
  left_join(
    pdat |> select(athlete, seasons_played = seasons),
    by = join_by(athlete)
  ) |>
  mutate(rookie = if_else(seasons_played == year(now()), "rookie", ""))  |> 
  arrange(desc(total_points)) |> 
  select(athlete, rookie, everything())

doubles24 <- SharedData$new(doubles24, group = "experience", key = ~athlete)
```

```{r comparedata}
# per-game and percentage stats for the player comparison table;
# not a SharedData - selection drives column visibility via the JS below
compare_raw <- dat |>
  filter(
    season == this_year,
    type_id == 1,
    !is.na(athlete_display_name)
  ) |>
  summarise(
    headshot = first(athlete_headshot_href[!is.na(athlete_headshot_href)]),
    # nickname rather than the full display name: it has to read under a 60px
    # photo, where "Golden State Valkyries" wraps to three lines
    team = team_name[which.max(game_date)],
    games = n_distinct(game_id, na.rm = TRUE),
    total_points = sum(points, na.rm = TRUE),
    ppg = total_points / games,
    apg = sum(assists, na.rm = TRUE) / games,
    rpg = sum(rebounds, na.rm = TRUE) / games,
    spg = sum(steals, na.rm = TRUE) / games,
    bpg = sum(blocks, na.rm = TRUE) / games,
    topg = sum(turnovers, na.rm = TRUE) / games,
    fg_pct = 100 * sum(field_goals_made, na.rm = TRUE) /
      sum(field_goals_attempted, na.rm = TRUE),
    tp_pct = 100 * sum(three_point_field_goals_made, na.rm = TRUE) /
      sum(three_point_field_goals_attempted, na.rm = TRUE),
    ft_pct = 100 * sum(free_throws_made, na.rm = TRUE) /
      sum(free_throws_attempted, na.rm = TRUE),
    fta = sum(free_throws_attempted, na.rm = TRUE),
    .by = athlete_display_name
  )

fmt1 <- function(x) if_else(is.finite(x), sprintf("%.1f", x), "—")
fmt_pct <- function(x) if_else(is.finite(x), sprintf("%.1f%%", x), "—")

photo_cell <- function(url, team) {
  portrait <- if_else(
    is.na(url),
    '<div style="width:60px;height:60px;border-radius:50%;background:#efe3c6;margin:0 auto;"></div>',
    paste0(
      '<img src="', url,
      '" style="width:60px;height:60px;border-radius:50%;object-fit:cover;background:#ffffff;" loading="lazy">'
    )
  )

  paste0(
    '<div class="compare-photo">',
    portrait,
    '<div class="compare-team">', coalesce(team, "&mdash;"), '</div>',
    "</div>"
  )
}

# column order is fixed at render time, so arranging by ppg here keeps any
# selection of columns sorted left-to-right by descending points per game
compare_wide <- compare_raw |>
  arrange(desc(ppg)) |>
  transmute(
    athlete = athlete_display_name,
    Photo = photo_cell(headshot, team),
    `Points / Game` = fmt1(ppg),
    `Assists / Game` = fmt1(apg),
    `Rebounds / Game` = fmt1(rpg),
    `Steals / Game` = fmt1(spg),
    `Blocks / Game` = fmt1(bpg),
    `Turnovers / Game` = fmt1(topg),
    `FG%` = fmt_pct(fg_pct),
    `3P%` = fmt_pct(tp_pct),
    `FT%` = fmt_pct(ft_pct),
    `FT Attempted` = as.character(fta)
  ) |>
  pivot_longer(-athlete, names_to = "stat", values_to = "value") |>
  # the headshots need no row label; "Photo" only stated the obvious. The name
  # has to survive as a column above so pivot_longer has something to key on.
  mutate(stat = if_else(stat == "Photo", "", stat)) |>
  pivot_wider(names_from = athlete, values_from = value)

compare_players <- setdiff(names(compare_wide), "stat")

compare_top10 <- compare_raw |>
  arrange(desc(total_points)) |>
  slice_head(n = 10) |>
  pull(athlete_display_name)

# crosstalk selection keys are athlete names, which are exactly the
# compare-table column ids, so no key-to-column mapping is needed
compare_js <- paste0(
  "(function() {\n",
  "  var players = ", jsonlite::toJSON(compare_players), ";\n",
  "  var top10 = ", jsonlite::toJSON(compare_top10), ";\n",
  "  var clearBtns = [];\n",
  "  function apply(keys) {\n",
  "    var shown = (keys && keys.length) ? keys : top10;\n",
  "    var hidden = players.filter(function(p) { return shown.indexOf(p) === -1; });\n",
  "    if (window.Reactable) { Reactable.setHiddenColumns('compare-table', hidden); }\n",
  "  }\n",
  "  // A selection filters every page, so picking someone who has no season on\n",
  "  // the page you switch to leaves that page with no row to untick -- e.g.\n",
  "  // Sue Bird, last season 2022, empties the current-year page completely.\n",
  "  // The way out therefore has to sit outside the table, in its card header,\n",
  "  // and on both pages, since either one can be the page left empty.\n",
  "  function showClear(active) {\n",
  "    clearBtns.forEach(function(b) { b.hidden = !active; });\n",
  "  }\n",
  "  function buildClear(handle) {\n",
  "    return ['main-table', 'all-time-table'].map(function(id) {\n",
  "      var tbl = document.getElementById(id);\n",
  "      var header = tbl && tbl.closest('.card')\n",
  "        ? tbl.closest('.card').querySelector('.card-header')\n",
  "        : null;\n",
  "      if (!header) { return null; }\n",
  "      var btn = document.createElement('button');\n",
  "      btn.type = 'button';\n",
  "      btn.className = 'card-clear';\n",
  "      btn.textContent = '\\u2715 Clear selection';\n",
  "      btn.hidden = true;\n",
  "      btn.addEventListener('click', function() { handle.set(null); });\n",
  "      header.appendChild(btn);\n",
  "      return btn;\n",
  "    }).filter(Boolean);\n",
  "  }\n",
  "  window.addEventListener('load', function() {\n",
  "    var handle = new crosstalk.SelectionHandle('experience');\n",
  "    clearBtns = buildClear(handle);\n",
  "    handle.on('change', function(e) {\n",
  "      // deselecting the last row broadcasts [] , which plotly renders as\n",
  "      // all-dimmed; plotly restores full opacity only on a strict null\n",
  "      if (e.value && e.value.length === 0) { handle.set(null); return; }\n",
  "      apply(e.value);\n",
  "      showClear(!!(e.value && e.value.length));\n",
  "    });\n",
  "    apply(handle.value);\n",
  "    showClear(!!(handle.value && handle.value.length));\n",
  "  });\n",
  "})();"
)
```

```{r dribble-js}
# The instruction on the two player tables is the one thing a first-time
# visitor has to notice, and a plain card title did not carry it. A trail of
# basketballs now curves out of the end of the text and drops onto the top
# edge of the table it is talking about. Quarto builds the card header, so
# the arc has to be appended to it here rather than emitted with the table.
#
# Coordinates are five points sampled off the quadratic curve
# M2,14 Q78,14 110,42 at t = .15 .38 .6 .8 1, so the balls sit on a real
# path instead of an eyeballed scatter. The curve is deliberately shallow:
# the card clips at its own top edge, and a steeper arc put the first dot
# outside it. Everything else visual -- colors, the bounce, the staggered
# fade-in -- lives in styles.scss.
dribble_arc <- paste0(
  "<svg class='dribble-arc' viewBox='0 0 132 56' width='132' height='56' aria-hidden='true' focusable='false'>",
  "<circle class='dribble-dot' style='--dot-o:.35;animation-delay:.02s' cx='23.8' cy='14.6' r='2.1'/>",
  "<circle class='dribble-dot' style='--dot-o:.50;animation-delay:.10s' cx='53.4' cy='18.0' r='3.1'/>",
  "<circle class='dribble-dot' style='--dot-o:.65;animation-delay:.18s' cx='77.4' cy='24.1' r='4.3'/>",
  "<circle class='dribble-dot' style='--dot-o:.80;animation-delay:.26s' cx='95.4' cy='31.9' r='6.0'/>",
  "<g class='dribble-ball'>",
  "<circle class='dribble-halo' cx='110' cy='42' r='12.4'/>",
  "<circle class='dribble-skin' cx='110' cy='42' r='9.5'/>",
  "<path class='dribble-seam' d='M110 32.5v19M100.5 42h19",
  "M103.3 35.3c3.7 3.7 3.7 9.4 0 13.1M116.7 35.3c-3.7 3.7-3.7 9.4 0 13.1'/>",
  "</g>",
  "</svg>"
)

dribble_js <- paste0(
  "(function() {\n",
  "  var arc = \"", dribble_arc, "\";\n",
  "  window.addEventListener('load', function() {\n",
  "    ['main-table', 'all-time-table'].forEach(function(id) {\n",
  "      var tbl = document.getElementById(id);\n",
  "      var card = tbl ? tbl.closest('.card') : null;\n",
  "      var title = card ? card.querySelector('.card-header .card-title') : null;\n",
  "      if (!title) { return; }\n",
  "      var slot = document.createElement('span');\n",
  "      slot.className = 'dribble';\n",
  "      slot.innerHTML = arc;\n",
  "      title.appendChild(slot);\n",
  "    });\n",
  "  });\n",
  "})();"
)
```

```{r race-prep}
# cumulative points by game date for the scoring race chart
race <- dat |>
  filter(
    season == this_year,
    type_id == 1,
    !is.na(athlete_display_name)
  ) |>
  select(athlete_display_name, game_date, points) |>
  mutate(game_date = as_date(game_date)) |>
  arrange(game_date) |>
  mutate(
    cum_points = cumsum(replace_na(points, 0)),
    .by = athlete_display_name
  ) |>
  rename(athlete = athlete_display_name) |>
  SharedData$new(group = "experience", key = ~athlete)
```

```{r efficiency-prep}
# true shooting percentage vs shot volume for the efficiency chart;
# floor of 50 field goal attempts keeps tiny-sample players out
efficiency <- dat |>
  filter(
    season == this_year,
    type_id == 1,
    !is.na(athlete_display_name)
  ) |>
  summarise(
    n_games = n_distinct(game_id, na.rm = TRUE),
    fga = sum(field_goals_attempted, na.rm = TRUE),
    fta = sum(free_throws_attempted, na.rm = TRUE),
    n_points = sum(points, na.rm = TRUE),
    .by = athlete_display_name
  ) |>
  filter(fga >= 50) |>
  mutate(
    fga_per_game = round(fga / n_games, 1),
    ts_pct = round(100 * n_points / (2 * (fga + 0.44 * fta)), 1)
  ) |>
  rename(athlete = athlete_display_name) |>
  SharedData$new(group = "experience", key = ~athlete)
```

```{r experience-prep}

# named to avoid shadowing the `seasons` season-range vector defined above.
# The type_id filter matters: this plot's y axis reads "Regular Season Games
# Played", but without it playoff and Commissioner's Cup games were counted
# too, overstating 235 careers by as much as 58 games.
experience <- dat |>
  filter(type_id == 1) |>
  summarise(
    n_seasons = n_distinct(season, na.rm = TRUE),
    n_games = n_distinct(game_id, na.rm = TRUE),
    .by = c(athlete_display_name)
  ) |>
  arrange(n_seasons) |>
  rename(athlete = athlete_display_name) |>
  drop_na(athlete)

# longest career in the data, used to size the experience plot x axis
max_seasons <- max(experience$n_seasons, na.rm = TRUE)

player_experience <- experience |>
  SharedData$new(group = "experience", key = ~athlete)

points <- dat |> 
  filter(
    season == as.integer(year(now())),
    type_id == 1
    ) |>
  summarise(
    n_games = n_distinct(game_id, na.rm = TRUE),
    n_points = sum(points, na.rm = TRUE),
    .by = c(athlete_display_name)
  ) |> 
  arrange(desc(n_games)) |> 
  rename(athlete = athlete_display_name) |> 
  drop_na(athlete) |> 
  SharedData$new(group = "experience", key = ~athlete)

```

```{r statsdata}
# dfs for all time stats

stat_names <- c(
  "total_games", "total_points", "goals_made", "threes_made",
  "total_assists", "total_rebounds", "total_steals",
  "total_blocks", "total_turnovers", "total_fouls"
)

stat_dfs <- stat_names %>%
  set_names() %>%
  map(~ stat(pdat, col = {{ .x }}))


# dfs for this_year stats

stat24_dfs <- stat_names %>%
  set_names() %>%
  map(~ stat(pdat24, col = {{ .x }}))

# https://rlang.r-lib.org/reference/topic-inject.html#embracing-with-
```

```{r sizes}
# max width for columns in reactables
sm <- 50
md <- 70
lg <- 110
```

```{r tableoptions}

wnba_orange <- "#f57b20"
wnba_dark_orange <- "#e96500"
wnba_tan <- "#efe3c6"
wnba_gray <- "#4c4c4d"
wnba_black <- "#050707"

court_ratio <- 1/1.88

doubles_theme <- reactableTheme(
  color = "#FFFFFF",
  backgroundColor = wnba_orange,
  borderColor = wnba_tan,
  highlightColor = wnba_black,
  inputStyle = list(backgroundColor = wnba_dark_orange),
  selectStyle = list(backgroundColor = wnba_orange),
  pageButtonHoverStyle = list(backgroundColor = wnba_black),
  pageButtonActiveStyle = list(backgroundColor = wnba_black)
)

compare_theme <- reactableTheme(
  color = wnba_tan,
  backgroundColor = wnba_black,
  borderColor = wnba_gray
)

stat_theme <- reactableTheme(
  backgroundColor = wnba_tan,
  borderColor = wnba_tan,
  highlightColor = wnba_black,
  rowHighlightStyle = list(color = wnba_tan),
  inputStyle = list(backgroundColor = "#FFFFFF"),
  pageButtonHoverStyle = list(backgroundColor = wnba_black, color = wnba_tan),
  pageButtonActiveStyle = list(backgroundColor = wnba_black)
)
```

# `r this_year`

## Row {height=400px}

### Column

```{r doubles24}
#| title: Click a row to filter everything
doubles24 |>
  reactable(
    elementId = "main-table",
    compact = FALSE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    selection = "multiple",
    onClick = "select",
    rowStyle = list(cursor = "pointer"),
    theme = doubles_theme,
    columns = list(
      athlete = colDef(name = "Athlete", maxWidth = md, filterable = TRUE),
      rookie = colDef(name = "Rookie", maxWidth = md, filterable = TRUE),
      double_double = colDef(name = "Double Dbl", maxWidth = md),
      triple_double = colDef(name = "Triple Dbl", maxWidth = md),
      total_points = colDef(name = "Total Points", maxWidth = md),
      ppg = colDef(
        name = "Per Game",
        minWidth = 300,
        cell = function(values) {
          sparkline(rev(values), type = "bar", chartRangeMin = 0, chartRangeMax = max(dat$points, na.rm = TRUE), width = 240, barColor = "#ffffff", barWidth = 4, barSpacing = 1, zeroColor = "#999999", nullColor = "#000000")
        }
      ),
      position = colDef(name = "Position", filterable = TRUE),
      latest_team = colDef(name = "Team", filterable = TRUE),
      seasons_played = colDef(name = "Seasons Played")
    )
  )
```

### Column {.tabset}

```{r compare}
#| title: Compare
htmltools::tagList(
  reactable(
    compare_wide,
    elementId = "compare-table",
    sortable = FALSE,
    pagination = FALSE,
    highlight = FALSE,
    compact = TRUE,
    theme = compare_theme,
    defaultColDef = colDef(
      show = FALSE,
      html = TRUE,
      align = "center",
      vAlign = "center",
      minWidth = 100
    ),
    columns = c(
      list(
        stat = colDef(
          show = TRUE,
          html = FALSE,
          name = "",
          align = "left",
          sticky = "left",
          minWidth = lg,
          style = list(fontWeight = "600")
        )
      ),
      map(set_names(compare_top10), \(x) colDef(show = TRUE))
    )
  ),
  htmltools::div(
    class = "compare-hint",
    "Top 10 by total points shown by default — select players in the left table to compare"
  ),
  htmltools::tags$script(htmltools::HTML(compare_js)),
  htmltools::tags$script(htmltools::HTML(dribble_js))
)
```

```{r race}
#| title: Scoring Race
race_plot <- race |>
  ggplot(aes(x = game_date, y = cum_points, group = athlete)) +
  geom_line(
    color = wnba_orange,
    linewidth = 0.3,
    alpha = 0.4
  ) +
  labs(
    title = "Cumulative Points this Regular Season",
    subtitle = "each line is a player - select players in the tables",
    x = NULL,
    y = "Points Scored"
  ) +
  theme_light() +
  theme(
    legend.position = "none",
    text = element_text(color = wnba_tan),
    axis.text = element_text(color = wnba_tan),
    axis.line = element_blank(),
    axis.text.x = element_text(size = 8),
    axis.title = element_text(size = 10),
    panel.grid = element_blank(),
    panel.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.title = element_text(size = 14),
    plot.margin = unit(c(20, 40, 20, 40), "points")
  )

ggplotly(
  race_plot,
  height = 330,
  tooltip = c("athlete", "game_date", "cum_points")
) |>
  highlight(
    color = wnba_black,
    opacityDim = 0.6,
    selected = attrs_selected(line = list(width = 3))
  )

```

```{r efficiency}
#| title: Efficiency
efficiency_plot <- efficiency |>
  ggplot(aes(x = fga_per_game, y = ts_pct, group = athlete)) +
  geom_point(
    size = 2,
    color = wnba_orange
  ) +
  labs(
    title = "Shooting Efficiency vs Volume",
    subtitle = "each dot is a player with 50+ field goal attempts - select players in the tables",
    x = "Field Goal Attempts per Game",
    y = "True Shooting %"
  ) +
  theme_light() +
  theme(
    legend.position = "none",
    text = element_text(color = wnba_tan),
    axis.text = element_text(color = wnba_tan),
    axis.line = element_blank(),
    axis.text.x = element_text(size = 8),
    axis.title = element_text(size = 10),
    panel.grid = element_blank(),
    panel.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.title = element_text(size = 14),
    plot.margin = unit(c(20, 40, 20, 40), "points")
  )

ggplotly(
  efficiency_plot,
  height = 330,
  tooltip = c("athlete", "fga_per_game", "ts_pct")
)

```

```{r points}
#| title: Points this Season
points_plot <- points |>
  ggplot(aes(x = n_games, y = n_points, group = athlete)) +
  geom_point(
    size = 2,
    color = wnba_orange
  )  +
  labs(
    title = "Points this Regular Season",
    subtitle = "each dot is a player - select players in the tables",
    x = "Regular Season Games Played",
    y = "Points Scored"
  ) +
  scale_x_discrete(
    limits = factor(c(1:(num_games + 1))),
    breaks = as.character(c(1:(num_games)))
  ) +
  theme_light() +
  theme(
    legend.position = "none",
    text = element_text(color = wnba_tan),
    axis.text = element_text(color = wnba_tan),
    axis.line = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank(),
    axis.text.x = element_text(size = 8, angle = 300, hjust = 0),
    axis.title = element_text(size = 10),
    panel.grid = element_blank(),
    panel.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.title = element_text(size = 14),
    plot.margin = unit(c(20, 40, 20, 40), "points"),
    aspect.ratio = court_ratio
  )

ggplotly(
  points_plot,
  height = 330,
  tooltip = c("athlete", "n_games", "n_points")
)

```

## Row {height=300px}

```{r tp24}
tp24 <- stat24_dfs$total_points |>
  SharedData$new(key = ~athlete, group = "experience")

tp24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      total_points = colDef(
        name = "POINTS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r ta24}
ta24 <- stat24_dfs$total_assists |>
  SharedData$new(key = ~athlete, group = "experience")

ta24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      total_assists = colDef(
        name = "ASSISTS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r tr24}
tr24 <-stat24_dfs$total_rebounds |>
  SharedData$new(key = ~athlete, group = "experience")

tr24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      total_rebounds = colDef(
        name = "REBOUNDS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r ts24}
ts24 <- stat24_dfs$total_steals |>
  SharedData$new(key = ~athlete, group = "experience")

ts24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      total_steals = colDef(
        name = "STEALS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

## Row {height=300px}

```{r tb24}
tb24 <- stat24_dfs$total_blocks |>
  SharedData$new(key = ~athlete, group = "experience")

tb24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      total_blocks = colDef(
        name = "BLOCKS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r gm24}
gm24 <- stat24_dfs$goals_made |>
  SharedData$new(key = ~athlete, group = "experience")

gm24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      goals_made = colDef(
        name = "GOALS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r tm24}
tm24 <- stat24_dfs$threes_made |>
  SharedData$new(key = ~athlete, group = "experience")

tm24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      threes_made = colDef(
        name = "THREES",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r tt24}
tt24 <- stat24_dfs$total_turnovers |>
  SharedData$new(key = ~athlete, group = "experience")

tt24 |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    defaultSorted = list(rank = "asc"),
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      seasons = colDef(show = FALSE),
      total_turnovers = colDef(
        name = "T-OVERS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

# All Time

## Row {height=400px}

```{r alldoubles}
#| title: Click a row to filter everything
doubles |>
  reactable(
    elementId = "all-time-table",
    compact = FALSE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    selection = "multiple",
    onClick = "select",
    rowStyle = list(cursor = "pointer"),
    columns = list(
      athlete = colDef(name = "Athlete", maxWidth = md, filterable = TRUE),
      rookie = colDef(name = "Rookie", maxWidth = md, filterable = TRUE),
      double_double = colDef(name = "Double Dbl", maxWidth = md),
      triple_double = colDef(name = "Triple Dbl", maxWidth = md),
      total_points = colDef(name = "Total Points", maxWidth = md),
      pps = colDef(
        name = "Per Season",
        minWidth = 153,
        maxWidth = 155,
        cell = function(values) {
          sparkline(values, type = "bar", width = 153, chartRangeMin = 0, barColor = "#ffffff", barWidth = 4, barSpacing = 1, zeroColor = "#999999", nullColor = "#000000")
        }
      ),
      position = colDef(name = "Position", filterable = TRUE),
      latest_team = colDef(name = "Team", filterable = TRUE),
      total_games = colDef(name = "Total Games"),
      total_seasons = colDef(name = "Total Seasons"),
      seasons = colDef(name = "Seasons")
    ),
    theme = doubles_theme
  )
```

```{r experience}
seasons_plot <- player_experience |>
  ggplot(aes(x = n_seasons, y = n_games, group = athlete)) +
  geom_point(size = 2, color = wnba_orange)  +
  labs(
    title = "Time on the Court",
    subtitle = "each dot is a player - select players in the tables",
    x = "Seasons Played",
    y = "Regular Season Games Played"
  ) +
  scale_x_discrete(
    limits = factor(seq_len(max_seasons)),
    breaks = as.character(seq_len(max_seasons))
  ) +
  theme_light() +
  theme(
    legend.position = "none",
    text = element_text(color = wnba_tan),
    axis.text = element_text(color = wnba_tan),
    axis.line = element_blank(),
    axis.text.y = element_blank(),
    axis.ticks.y = element_blank(),
    axis.text.x = element_text(size = 10),
    axis.title = element_text(size = 10),
    panel.grid = element_blank(),
    panel.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.background = element_rect(fill = wnba_black, color = wnba_black),
    plot.title = element_text(size = 14),
    plot.margin = unit(c(20, 40, 20, 40), "points"),
    aspect.ratio = court_ratio
  )

ggplotly(seasons_plot, tooltip = c("athlete", "n_seasons", "n_games"))

```

## Row {height=300px}

```{r tp}
tp <- stat_dfs$total_points |>
  SharedData$new(key = ~athlete, group = "experience")

tp |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      total_points = colDef(
        name = "POINTS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r ta}
ta <- stat_dfs$total_assists |>
  SharedData$new(key = ~athlete, group = "experience")

ta |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      total_assists = colDef(
        name = "ASSISTS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r tr}
tr <- stat_dfs$total_rebounds |>
  SharedData$new(key = ~athlete, group = "experience")

tr |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      total_rebounds = colDef(
        name = "REBOUNDS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

## Row {height=300px}

```{r ts}
ts <-stat_dfs$total_steals |>
  SharedData$new(key = ~athlete, group = "experience")

ts |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      total_steals = colDef(
        name = "STEALS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r tb}
tb <- stat_dfs$total_blocks |>
  SharedData$new(key = ~athlete, group = "experience")

tb |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      total_blocks = colDef(
        name = "BLOCKS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r gm}
gm <- stat_dfs$goals_made |>
  SharedData$new(key = ~athlete, group = "experience")

gm |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      goals_made = colDef(
        name = "GOALS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

## Row {height=300px}

```{r tm}
tm <- stat_dfs$threes_made |>
  SharedData$new(key = ~athlete, group = "experience")

tm |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      threes_made = colDef(
        name = "THREES",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```

```{r tt}
tt <- stat_dfs$total_turnovers |>
  SharedData$new(key = ~athlete, group = "experience")

tt |> 
  reactable(
    compact = TRUE,
    highlight = TRUE,
    filterable = FALSE,
    defaultPageSize = 20,
    theme = stat_theme,
    columns = list(
      rank = colDef(maxWidth = sm),
      total_seasons = colDef(name = "total seasons", maxWidth = md),
      seasons = colDef(maxWidth = lg),
      total_turnovers = colDef(
        name = "T-OVERS",
        headerClass = "my-header3",
        style = list(fontWeight = "bold")
      )
    )
  )
```


# Methods

::: {.card title="How games and points are calculated"}

{{< include METHODOLOGY.md >}}

:::
